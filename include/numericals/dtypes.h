#ifndef NUMERICALS_DTYPES_H
#define NUMERICALS_DTYPES_H

#include <cstddef>
#include <vector>

namespace numericals
{

using shape_t = std::vector<std::size_t>;
using strides_t = std::vector<std::size_t>;

} // namespace numericals

#endif // NUMERICALS_DTYPES_H